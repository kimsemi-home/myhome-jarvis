# ADR 0001: Local First

## Status

Accepted

## Decision

`myhome-jarvis` runs locally first, defaults to localhost and dry-run behavior,
and treats external services as optional integrations with offline fallback.
